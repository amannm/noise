[project]
name = "noise"
version = "0.1.0"
description = "AC noise detector using audio analysis"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "librosa",
    "scikit-learn",
    "joblib",
    "sounddevice",
    "soundfile",
    "pyyaml",
]
